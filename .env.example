# Public app configuration supplied once by the Framecut operator.
# End users should never need to enter these values in the app.
# Never add an OAuth client secret here.
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_API_KEY=
VITE_GOOGLE_APP_ID=
